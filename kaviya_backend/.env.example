# Backend server configuration
PORT=3000

# JWT secret (set a strong value in real environments)
# REQUIRED: Provide a non-empty secret
JWT_SECRET=changeme-please

# MongoDB connection string
# Example values:
# For docker-compose with service name:
# MONGODB_URI=mongodb://appuser:dbuser123@kaviya_database:5000/myapp?authSource=admin
# For local development:
# MONGODB_URI=mongodb://appuser:dbuser123@localhost:5000/myapp?authSource=admin
MONGODB_URI=

# CORS allowed origins (comma-separated). Use explicit origins for credentials support.
# For local dev frontend on same host:
ALLOWED_ORIGINS=http://localhost:3000
