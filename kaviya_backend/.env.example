# Backend server configuration
PORT=3001

# JWT secret for signing tokens (change in production)
JWT_SECRET=change-this-secret

# MongoDB connection
# Prefer overriding via environment variable. Defaults are for local/dev.
# Service name default (e.g., in docker-compose): kaviya_database
# localhost fallback if running DB locally
MONGODB_URI=mongodb://appuser:dbuser123@kaviya_database:5000/myapp?authSource=admin
