# Server configuration
PORT=3001
# JWT secret used to sign tokens (set a strong random value in real environments)
JWT_SECRET=change_me_development_secret

# MongoDB connection (service name within the environment)
# Primary (container/service):
MONGODB_URI=mongodb://appuser:dbuser123@kaviya_database:5000/myapp?authSource=admin
# Local fallback example (uncomment to use locally):
# MONGODB_URI=mongodb://appuser:dbuser123@localhost:5000/myapp?authSource=admin

# Optional: control DB host hint used by getMongoUri()
# KAVIYA_DB_HOST=localhost
